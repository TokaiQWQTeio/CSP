# CSP-J 第二轮集训专题

面向已学过 C++ 基本语法、准备 CSP-J 第二轮上机认证的学生。本文按知识依赖安排专题，不规定每个专题的课时。每个专题都应包含讲解、独立编程、测试与复盘；学生能够解释思路、复杂度并独立处理边界后，再进入下一专题。

配套练习见 [CSP-J 洛谷专题题单](CSP-J-洛谷专题题单.md)，题目与下列七个专题逐一对应。

这里的“典型题型”是训练方向，不表示某类题每年必考。专题内容以 [NOI 大纲（2025 年修订版）](https://www.noi.cn/xw/2025-04-18/841584.shtml)的入门级要求为范围依据，并参考 [CCF 的 2025 年 CSP-J 第二轮题目分析报告](https://www.noi.cn/upload/resources/file/2025/11/14/CSP-JS2025%E5%85%A5%E9%97%A8%E7%BA%A7%E9%A2%98%E7%9B%AE%E7%9F%A5%E8%AF%86%E6%9E%84%E6%88%90%E5%88%86%E6%9E%90%E6%8A%A5%E5%91%8A.pdf)安排综合训练。

## 1. C++ 实现基础

- **先修要求**：会使用变量、条件、循环，并能编译运行简单 C++ 程序。
- **教学目标**：把题意准确转成程序，写出可编译、结果正确且能处理边界的代码。
- **知识点**：一维与二维数组、下标和初始化；`string` 与字符处理；函数参数及返回值；`vector`、`pair`、`sort`、`min`、`max` 等常用工具；整数范围与 `long long`；标准输入输出、文件读写要求及本地测试。
- **典型题型**：逐项统计、按规则更新数组或字符串、二维表格处理、排序后输出结果。
- **易错点**：数组越界、下标从 0 或 1 开始时混用、整数溢出、输入数据未读完、特殊值和空结果未处理；本地运行正常但提交文件名或输入输出方式不符合题面。

## 2. 枚举与模拟

- **先修要求**：完成专题 1，能独立写多层循环和函数。
- **教学目标**：将条件拆成可检查的步骤，枚举不重不漏，并根据数据范围判断做法是否可行。
- **知识点**：单变量与多变量枚举；枚举对象和顺序的选择；分类讨论；过程模拟与状态维护；时间复杂度的初步估算。
- **典型题型**：寻找满足条件的方案、日期或坐标规则模拟、按事件顺序更新状态、枚举答案后检验。
- **易错点**：遗漏边界情况、重复计数、更新顺序错误、在循环中修改了尚未使用的状态、只看样例而忽略最大数据范围。

## 3. 序列处理

- **先修要求**：掌握数组、排序及基础复杂度分析。
- **教学目标**：从重复计算中发现可预处理的量，并能选择适合有序性或单调性的算法。
- **知识点**：排序与自定义比较；计数排序的适用条件；一维前缀和、差分及区间含义；双指针与滑动窗口的基本条件；在有序数组或单调答案上二分。
- **典型题型**：区间求和、多次区间加减、两数配对、满足条件的最短或最长区间、最小可行值。
- **易错点**：前缀和的 `r` 与 `l-1`、差分末尾位置、二分区间不收缩、双指针缺少单调条件、自定义排序比较器不一致。

## 4. 递归与搜索

- **先修要求**：会定义函数，理解数组或其他容器如何表示状态。
- **教学目标**：能画出搜索树，正确维护“当前状态—下一步—撤销”，并区分深度优先与广度优先搜索的用途。
- **知识点**：递归终止条件；回溯；DFS 与 BFS；`visited` 标记；网格和简单图的搜索；用合法性判断与已知上界做简单剪枝。
- **典型题型**：排列或组合生成、迷宫可达性、连通块计数、无权图最短步数、有限状态的方案搜索。
- **易错点**：没有终止条件、回溯时忘记恢复状态、该标记时未标记导致重复搜索、BFS 入队时机错误、把 DFS 首次到达当作最短路。

## 5. 基础数据结构

- **先修要求**：掌握数组、基础搜索和循环不变量。
- **教学目标**：根据操作需求选容器，并能正确表示树与图。
- **知识点**：栈、队列的后进先出与先进先出；`set`、`map` 及其查找、去重、计数用途；树的父子关系；图的点、边与邻接表；基本遍历。
- **典型题型**：括号或操作序列处理、待处理任务队列、频次统计、去重、树上节点统计、图的连通性。
- **易错点**：空容器取值、误以为所有容器操作都是常数时间、有向边与无向边混淆、树的根和叶子判断错误、节点编号不连续时直接当数组下标。

## 6. 贪心与基础动态规划

- **先修要求**：完成排序、枚举和基础递归专题，能说明解法的复杂度。
- **教学目标**：对贪心选择给出理由或反例；对动态规划写清状态、转移、初值和计算顺序。
- **知识点**：排序后贪心、区间类贪心的基本模式；反例检验与交换思路；递推；简单一维 DP；0/1 背包等基础背包模型。
- **典型题型**：按规则选择最多或最少对象、最优拆分、路径或序列上的最优值、有限容量下的最优选择。
- **易错点**：凭直觉使用贪心却无法证明；DP 状态含义不明确、不可达状态初值错误、背包更新方向写反、答案类型溢出。

## 7. 综合真题与整场训练

- **先修要求**：前六个专题均完成基础题；薄弱专题可在真题复盘后回补。
- **教学目标**：在完整题面和时间限制下独立选方法、实现、测试，并争取可验证的部分分。
- **知识点**：读题与数据范围分析；对每题列出朴素解、优化方向和预期复杂度；构造边界测试；提交前核对文件及编译结果；赛后复盘。
- **典型题型**：使用 [CCF 发布的历年 CSP-J/S 题目与数据](https://www.noi.cn/zxzy/lnzl/jszl/2025-12-15/854079.shtml)进行整套练习。2025 年 J 组分析涉及模拟、排序与贪心、位运算、前缀和及简单 DP，可作为多专题组合的复盘样本。
- **易错点**：只做会的题、不检查部分分条件、没有为边界和极值造测试、未保存并编译最终提交版本、复盘只记录答案而不记录错误原因。

建议每次复盘保留四项记录：原思路、出错输入、正确思路、下次如何检查。同一错误反复出现时，回到对应专题做针对性练习。

## 资料与使用说明

- [NOI 大纲（2025 年修订版）](https://www.noi.cn/xw/2025-04-18/841584.shtml)：确定入门级知识范围；专题顺序是教学安排，不是官方目录顺序。
- [2025 年 CSP-J 第二轮题目分析报告](https://www.noi.cn/upload/resources/file/2025/11/14/CSP-JS2025%E5%85%A5%E9%97%A8%E7%BA%A7%E9%A2%98%E7%9B%AE%E7%9F%A5%E8%AF%86%E6%9E%84%E6%88%90%E5%88%86%E6%9E%90%E6%8A%A5%E5%91%8A.pdf)：观察一套真题如何综合考查多个知识点及设置部分分。
- [历年题目与数据](https://www.noi.cn/zxzy/lnzl/jszl/2025-12-15/854079.shtml)：用于独立练习和赛后复盘；不要把单年题型当成下一年的预测。
